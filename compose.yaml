services:
  postgres:
    image: docker.io/library/postgres:16
    container_name: smart-check
    restart: always
    env_file:
      - ./.env
    ports:
      - 5432:5432
    volumes:
      - smart-check-vol:/usr/local/pgsql/data

volumes:
  smart-check-vol:
    external: true